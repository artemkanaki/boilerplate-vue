export const localeKey = 'locale';

export const routes = {
};

export enum Route {
  MainPage = '/',
  NotFound = '*'
}

export enum Locale {
  En = 'en',
}

export const STORE_SET_LOCALE = 'setLocale';
